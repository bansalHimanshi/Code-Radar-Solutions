#include <stdio.h>
int main()
{
    int a,b,c;
    scanf("%d",&c);
    for(a=0;a<=c;a++)
    {
        for(b=0;b<=a;b++)
        {
           // if(b==1 || b==c || a==b)
            
                printf("* ");
            
     printf("\n");   }
    }
}